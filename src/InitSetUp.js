import React from "react";

const InitSetUp = ({ date, church_branch_id, pastor_id }) => {
  return (
    <div>
      <p>Date: {date}</p>
      <p>Church Branch ID: {church_branch_id}</p>
      <p>Church Branch ID: {pastor_id}</p>
    </div>
  );
};

export default InitSetUp;
